THIS is sample JMS Spring integration using amq broker.

Build project
Start the server
Start the client

Send messages via client http://localhost:9080/send/{message}

The messages received via JMS at the server-side are logged to system.out.

